version: '3'

services:
  recipe-frontend:
    container_name: recipe-frontend
    build: 
      context: ./
      dockerfile: Dockerfile
    volumes:
      - .:/app
      - app/node_modules
    ports:
      - '3000:3000'